<h1>New event</h1>

<%= simple_form_for([@coures, @course.events.build], html: {class: "form-horizontal"}) do |f| %>
  <% if @course.events.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@course.events.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @course.events.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :due_date, :as => :date %>
  <%= f.input :worth %>
  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>

<%= link_to 'Back', courses_events_path %>
